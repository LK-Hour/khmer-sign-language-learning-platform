# Browser-side MediaPipe extracts keypoints; backend only runs NumPy MLP inference.
# Updated TensorFlow to 2.15+ for typing-extensions compatibility with fastapi/pydantic.
#   pip install -r requirements.txt -r requirements-ml.txt

h5py>=3.10,<4.0
mediapipe>=0.10
Pillow>=10.0
tensorflow==2.15.0
keras==2.15.0
numpy>=1.24.0,<1.25